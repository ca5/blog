name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master
    - name: setup
      run: pip install -r requirements.txt
    - name: run
      run: make
