name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master
    - name: test1
      run: python --version
    - name: test2
      run: python3 --version
    - name: setup
      run: pip install -r requirements.txt
    - name: run
      run: make
